from typing import Any


def prepare_documents(documents: list[dict[str, Any]]) -> list[dict[str, Any]]:
    # Connect OCR, document normalisation, or embedding preparation here.
    return documents
